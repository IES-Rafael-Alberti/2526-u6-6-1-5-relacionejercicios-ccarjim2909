# Ejercicio 6.3 — Incidencia de constructores en la herencia (RA7.c)

Basado en la teoría: “Incidencia de los constructores en la herencia”.

## Objetivo

Entender qué es un constructor en Kotlin, qué tipos existen y cómo afecta la **herencia** a la hora de definir y llamar constructores, mediante un ejercicio práctico con una pequeña jerarquía de clases.

## Entregables en este repositorio

- **Código Kotlin** en el paquete `es.ies.ejercicios.u6.ej63`.
- **Documentación** en este fichero `docs/ejercicios/6.3.md`, con:
  - explicación teórica aplicada a tu código,
  - salida por consola con “logs” (`println`),
  - **enlaces permanentes** a fragmentos de código (permalinks a líneas) en GitHub.

## Práctica (código)

Partiendo del material del paquete `es.ies.ejercicios.u6.ej63`, crea una jerarquía que cumpla:

a) **Superclase `open`** con:
   - **constructor primario** con al menos 2 parámetros.
   - bloque `init` que imprima un log indicando que se ha inicializado la superclase.
   - al menos **un constructor secundario** que delegue al primario con `this(...)`.

b) **Dos subclases** que hereden de la superclase y que incluyan:
   - constructor primario que invoque explícitamente al constructor de la superclase en la cabecera: `: NombreSuperclase(...)`.
   - al menos **un constructor secundario** en cada subclase que encadene con `this(...)` y, por tanto, acabe llamando al constructor de la superclase a través del primario.
   - logs en `init` y/o constructores para ver el **orden de inicialización** (superclase → subclase).

c) **Una subclase sin constructor primario** (caso especial) que incluya:
   - un **constructor secundario** que delegue directamente al **constructor padre**.

d) **Programa de demostración (`main`)** en este ejercicio:
   - Crea instancias usando **diferentes constructores** (primarios y secundarios).
   - Muestra por consola el orden de llamadas e inicialización.

## Guía paso a paso

1) Implementa la **superclase** con constructor primario + `init`.
2) Añade un **constructor secundario** en la superclase que delegue con `this(...)`.
3) Implementa la **primera subclase** con constructor primario que llame al constructor de la superclase como `: NombreSuperclase(...)` y añade logs.
4) Añade un **constructor secundario** en esa subclase que delegue con `this(...)`.
5) Repite con la **segunda subclase**.
6) Implementa la **subclase sin constructor primario** (solo con constructores secundarios) y completa el hueco de la delegación al constructor padre, pista: `super`. Recuerda añadir logs para ver el orden de inicialización.
7) Implementa/actualiza el `main` para instanciar usando distintas rutas de construcción.
8) Ejecuta y copia la salida en “Evidencias”.

## Teoría (responder después de terminar la guía)

a) ¿Qué es un constructor y para qué lo usamos?

RESPUESTA:

- Un constructor es un bloque de código que se ejecuta al crear un objeto y sirve para inicializar sus propiedades.\
    En mi jerarquía:\
    - Figura tiene un constructor primario que inicializa color y etiqueta:

                permalink
                open class Figura(
                val color: String,
                val etiqueta: String,
                )

- También tiene un constructor secundario que permite inicializar solo color y asigna una etiqueta por defecto:

                permalink
                constructor(color: String) : this(color, etiqueta = "sin-etiqueta")

- Las subclases Rectangulo y Circulo usan constructores primarios y secundarios para inicializar tanto las propiedades heredadas (color, etiqueta) como sus propias propiedades (ancho, alto, radio).

- Triangulo no tiene constructor primario; sus constructores secundarios delegan directamente al constructor de la superclase Figura:

        permalink
        constructor(base: Int, altura: Int) : super(color = "sin-color", etiqueta = "triangulo")

b) ¿Cuántos tipos de constructores nos podemos encontrar en Kotlin y cuáles son las diferencias?

RESPUESTA:

- En Kotlin hay dos tipos principales de constructores:
\
    **Constructor primario**\
    Se declara en la cabecera de la clase.\
    Sirve para inicializar propiedades principales.\
    Puede ser usado directamente por la subclase al heredar\
\
    **Ejemplo en mi código:**

        class Rectangulo(
        color: String,
        etiqueta: String,
        val ancho: Int,
        val alto: Int
        ) : Figura(color, etiqueta)

    **Constructor secundario**\
    Se declara dentro del cuerpo de la clase con la palabra constructor.\
    Sirve para sobrecargar formas de crear objetos, pudiendo asignar valores por defecto o simplificar la creación.\
    Debe delegar al constructor primario usando this(...) si existe, o directamente al constructor de la superclase usando super(...) si no hay primario.
    \
    Ejemplo en mi código:

constructor(ancho: Int, alto: Int) : this(
color = "sin-color",
etiqueta = "rectangulo",
ancho = ancho,
alto = alto
)

c) ¿De qué manera afecta y cómo hay que usar los constructores en la herencia?

RESPUESTA: *(Escribe tu respuesta aquí explicando por qué la subclase debe llamar al constructor de la superclase (en la cabecera `: NombreSuperclase(...)`) y el orden de inicialización, apoyándote en tus logs.)*

> Pista: explica también el caso “sin constructor primario”, donde un constructor secundario puede delegar directamente al constructor padre.

## Evidencias (salida por consola)

Pega aquí una ejecución breve que demuestre:

- Diferencias entre construir con constructor primario vs secundario.
- Orden de inicialización superclase → subclase.
- El caso de subclase sin constructor primario, delegando al constructor padre desde un secundario.

RESPUESTA: *(Pega aquí la salida por consola.)*

## Enlaces permanentes a código (obligatorio)

Incluye enlaces permanentes a:

- La superclase (constructores + `init`).
   RESPUESTA:

- Cada subclase (constructores + `init`).
   RESPUESTA:

- La subclase sin constructor primario y su constructor secundario delegando al constructor padre.
   RESPUESTA:

- El `main` que demuestra los casos.
   RESPUESTA:

## Condiciones de entrega (según enunciado)

- Se entrega la URL al ejercicio resuelto en este repositorio (con código y documentación). El enlace a este Markdown (`docs/ejercicios/6.3.md`) se puede usar como referencia en Moodle.

## Recursos

- Teoría (incidencia de constructores en la herencia): https://revilofe.github.io/section1/u06/teoria/PROG-U6.1.-jerarquiaDeClases/#4-incidencia-de-los-constructores-en-la-herencia
- Enlaces permanentes a código (GitHub Docs): https://docs.github.com/es/get-started/writing-on-github/working-with-advanced-formatting/creating-a-permanent-link-to-a-code-snippet

## Criterios (autoevaluación)

- He explicado qué es un constructor y para qué sirve.
- Distingo constructor primario y secundario con ejemplos del código.
- He demostrado cómo afecta la herencia a los constructores (con logs).
- Documento el ejercicio con permalinks y una presentación clara (identificación + fecha).
